// Top-level build file - projects/modules burada yapılandırılır
plugins {
    id 'com.android.application' version '8.2.0' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
